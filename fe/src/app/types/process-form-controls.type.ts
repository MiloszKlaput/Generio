import { FormControl } from "@angular/forms";

export type ProcessFormControls = {
  // jiraLogin: FormControl<string | null>;
  // jiraUserId: FormControl<string | null>;
  // jiraApiKey: FormControl<string | null>;
  // jiraUserJiraUrl: FormControl<string | null>;
  geminiApiKey: FormControl<string | null>;
  geminiMessage: FormControl<string | null>;
};
